<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WBattery" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/thema</item>
        <item name="colorPrimaryDark">@color/thema</item>
        <item name="colorAccent">@color/thema</item>
        <item name="android:windowBackground">@color/thema</item>
    </style>

    <style name="Theme.WBattery" parent="Base.Theme.WBattery">
        <item name="colorPrimary">@color/thema</item>
        <item name="colorPrimaryDark">@color/thema</item>
        <item name="colorAccent">@color/thema</item>
        <item name="android:windowBackground">@color/thema</item>

    </style>
</resources>